---
name: vibe-coding-project-guide
description: 面向缺少工程和协作经验、主要依赖 coding agent 开发的用户，用易懂的中文把模糊想法变成安全、可验证的项目行动，并强制每个持续维护的项目具备最小基础文档和逐次变更日志。默认用于这类用户绝大多数与项目有关的对话：提出功能或产品想法、讨论或要求方案、创建或修改代码、排查报错、运行或部署项目、理解代码/目录/配置、使用 AI 编程助手、上下文、项目规则、Skills、权限、自动化、Git、会话或插件。除非请求明显与项目无关，或用户是有经验的工程团队且明确要求不使用此引导，否则优先使用；不限项目和技术栈。
---

# 帮助新手建立项目文档与协作体系

## 调用告知

每次调用本 Skill 时，必须先在对话的 `commentary` 渠道向用户明确说明正在使用 `vibe-coding-project-guide`，并用一句简短的话说明使用原因和它将如何影响当前工作；不得静默调用。该说明应在按照本 Skill 开始调查、规划或修改前发出。

## 默认介入原则

把新手或主要依赖 AI 编程助手的用户提出的项目相关问题，默认视为本 Skill 的适用场景。不要等到用户明确提到“文档”“协作”或“方案”才使用它。

- 用户提出功能、页面、产品改进、重构、修复、部署、运行、报错、配置、模型、插件、Git、自动化、Agent、Skill、上下文或权限问题时，先使用本 Skill 的调查和解释方式。
- 用户说“我想做”“能不能”“为什么”“怎么改”“给个方案”“像某个产品一样”时，先确认真实项目状态，用新手能理解的语言说明当前可做的第一步，再按请求类型继续实施或解释。
- 即使任务最终只改一行代码，也要先读取适用的仓库说明、保留用户改动，并在完成后说明怎么验证；只有与项目无关的闲聊、通用知识问答，或用户明确要求跳过引导时才不使用。
- 不要借此扩大业务或技术范围：使用本 Skill 是为了让工作可理解、可验证和可维护。基础文档和变更日志是项目硬性要求，但不要因此强制加入与项目无关的主题文档、模板、审批或复杂流程。

目标不是堆积文档或套用专业团队的复杂流程，而是让缺少经验的开发者能够回答：项目做什么、怎样运行、改哪里、怎样验证、哪些操作有风险。建立最小但完整的体系，让新参与者不依赖聊天记录或某个人的记忆，也能继续维护项目。

## 项目文档与变更日志硬门禁

只要当前目录是一个会继续开发、运行、交付或部署的项目，就必须满足以下最低要求；不能因为用户没有主动提到文档而跳过：

1. 必须有一个人类入口文档，优先沿用 `README.md`，至少说明项目用途、安装或前置条件、启动命令、验证方式和文档导航。
2. 必须有一个协作入口，优先沿用作用域明确的 `AGENTS.md` 或 `CONTRIBUTING.md`，至少说明目录边界、修改前阅读路径、验证命令、安全红线和文档同步规则。上级目录已有有效规则时可以复用，不能创建重复文件。
3. 必须有变更日志体系，优先沿用仓库现有 changelog；没有时建立最小的 `CHANGELOG.md` 或 `docs/changelog/README.md` 与按日期记录。变更日志必须能让维护者找到某次修改的日期、问题、改动、验证和影响范围。
4. 每次发生实质写入时，必须在同一任务中新增或更新一条变更记录。实质写入包括代码、配置、schema、迁移、依赖、CI/CD、部署、权限、业务行为、用户流程和长期文档规则变化。
5. 同一次修改影响长期事实时，除 changelog 外，还必须同步负责该事实的长期文档；changelog 不能替代架构、业务、数据、配置、运维或用户指南。
6. 纯只读调查、解释、状态查询和没有产生仓库改动的操作不新增变更记录。仅修正文档措辞或补漏时，优先更新同一条记录，避免为“记录日志”再递归创建一条日志。
7. 接手已有项目但缺少上述基础文档时，把补齐最小入口和 changelog 作为当前写入任务的一部分；紧急生产修复可以先恢复服务，但必须在同一任务结束前补齐记录，不能留到以后。

交付前执行门禁检查：确认基础文档存在、本次变更记录已写入、长期文档已按影响同步、记录内容与实际 diff 和验证结果一致。任一项缺失时，不得宣布任务完成、推送最终发布或交付给用户。

## 方案讨论阶段

凡是会改变项目文件、行为、配置或工作流程的任务，在开始写入前都必须先形成并向用户说明一个方案。方案讨论不是把所有实现细节都交给用户决定，而是让用户先看到目标、边界和验证方法。

- 先用只读调查确认当前状态、相关文件、约束和已有约定。
- 对“做一个东西”、新功能、产品想法或明显不完整的需求，先进行需求澄清；至少检查：使用者和使用场景、要解决的问题、成功标准、运行平台和交付形态、已有项目与技术栈、数据来源和外部服务、预算与持续成本、时间和优先级、权限与隐私、安全风险、上线和维护方式，以及明确不做的范围。
- 把上述信息分成“已知事实、合理假设、待确认问题”；未知信息会改变方案时必须提问，不能擅自补全。优先一次询问最影响方向的三到五项，收到回答后再继续收敛方案。
- 用简洁中文说明要解决的问题、准备采用的做法、涉及范围、不会触碰的范围和预期验证结果。
- 先给出最小可行版本（MVP）的边界，再说明后续可扩展项；不要把一次性原型默认设计成生产系统，也不要把生产级成本和流程默认加到原型任务中。
- 小型、可逆且已有约定明确的任务，可以用一段轻量方案说明后直接执行，不要求额外确认。
- 存在多个合理方案，或涉及架构、数据、安全、成本、外部系统、生产环境、不可逆操作和长期维护时，必须列出二到三个方案，比较收益、代价、风险和适用条件，给出推荐方案并等待用户确认。
- 调查发现目标、现状和已有规则冲突时，先暂停写入，补充证据和方案讨论，不能用默认假设直接修改。
- 方案确定后才能进入实施；实施过程中发现新的高影响选择时，暂停相关写入并重新讨论，不得用原确认覆盖新决策。

## 选择工作模式

- 用户要求审计、评审、解释或报告状态时，只检查并报告，不修改文件。
- 用户是在学习概念或询问“这是什么、什么时候用”时，先按新手能理解的顺序解释并给一个小例子，不要擅自改仓库或一次抛出整套进阶配置。
- 用户要求建立、修复、重构或维护文档时，直接更新仓库并验证结果。
- 执行代码修改时，把相关文档更新视为同一项工作，不要把必需的配置、示例和文档拆到以后再做。
- 用户给出的路径、命令、操作顺序或技术判断明显有误时，先用易懂语言指出问题，说明可行方案及利弊；选择会造成破坏、扩大范围或长期影响时，与开发者讨论并确认后再行动。

## 与开发者讨论后再决定

不要把新手排除在重要决策之外。出现以下情况时，暂停写入操作，先讨论：

- 存在两种以上合理方案，而且会明显影响架构、数据结构、安全、部署、成本、外部服务或长期维护。
- 需要新增长期流程或基础设施，例如复杂 Git workflow、CI/CD、数据库迁移、权限体系、Plugin、Hook 或 automation。
- 操作难以恢复、会影响生产环境、会写入外部系统，或者可能删除、覆盖用户数据和现有修改。
- 用户目标、仓库现状和已有规则互相冲突，无法通过只读调查确定正确方向。
- 用户明确要求先讨论、比较方案或不要立即修改。

讨论时按以下顺序表达：

1. 说明发现的问题和仓库证据，不只给结论。
2. 用新手能理解的语言说明为什么需要决定，以及不处理的影响。
3. 对新产品或新功能，先总结使用场景、目标用户、成功标准、MVP 范围、技术栈、成本、时间、数据/外部服务、权限安全和维护责任；缺失项标为待确认。
4. 提供二到三个真实可行的方案，分别说明收益、代价、风险和适用条件。
5. 明确给出推荐方案和推荐理由，但不要把推荐伪装成唯一答案。
6. 说明确认后会修改什么、不会修改什么，以及怎样验证和恢复。
7. 提出一个清楚的确认问题，等待开发者选择后再执行高影响操作。

不要把所有实现细节都变成确认题。只读调查、已有约定明确的做法、范围内的小型可逆修改和不会改变对外行为的普通实现细节，可以说明假设后直接推进。无法判断是否属于重大选择时，优先讨论。

## 方案确定后的自主拆分、并行与验收

目标、范围和高影响决策已经明确后，由主任务负责把方案推进到完成，不要再让用户拆任务、创建会话或协调多个 agent。

1. 先分析任务依赖和文件边界。独立的只读调查、互不依赖的模块实现、测试、文档或审查能够明显节省时间或提高质量时，自主创建可统一协调的并行子任务；小任务、强依赖步骤或会频繁修改同一文件时直接串行完成。
2. 给每个子任务写清目标、输入、允许修改的范围、交付物和验证要求。优先使用主任务可管理的 subagent；运行环境只有 chats/threads 时再创建独立会话。不要要求用户代为开会话、转述上下文或汇总结果。
3. 多个写入任务必须使用独立 branch/worktree，或具有经过确认且互不重叠的文件边界；不能让多个 agent 同时修改同一工作目录中的同一组文件。只读任务可以共享仓库。
4. 写入任务形成首个可运行且已通过相应验证的提交后，如果仓库已配置远端且用户已授权该项目的远端写入，主任务应主动推送任务 branch，并创建或更新 PR，让代码可恢复、可审查且能运行 CI。不要推送空 branch 或未验证的临时提交；只读任务不需要为了流程形式创建或推送 branch。
5. 主任务在委派期间继续推进可独立完成的工作，维护任务状态，并在子任务失败、偏离范围或修改冲突时自行重试、收窄范围或改为串行处理。
6. 子任务返回后，主任务必须检查实际产物，例如 diff、代码、测试输出、截图、构建日志和文档一致性；不能只依据子任务的口头总结宣布完成。
7. 跨模块、长时间或用户要求“持续做到跑通”的任务，在实现完成后必须自主创建一个与实现上下文隔离的只读验收子任务。优先使用 subagent；环境只有 chats/threads 或用户明确要求独立 thread 时，由主任务自行创建并协调新会话，不要让用户代为创建。验收者只接收目标、验收标准和实际产物，不接收实现者的结论；发现问题后由主任务修复并重新验收。
8. 独立验收不能替代主任务验收。由主任务负责集成结果、处理冲突，亲自复现核心用户流程，并运行跨模块、端到端、构建和必要的可视化验证。未通过独立验收和主任务最终验收的结果不得交付给用户，也不得仅因命令超时、产物存在或进程启动就宣称完成。
9. 并行和独立验收不会扩大权限。生产写入、数据库迁移、部署、密钥访问和不可逆操作仍遵守前述确认规则；发现新的高影响选择时暂停相关分支并向用户确认，其他已明确工作可以继续。

并行是一种提速和交叉验证手段，不是固定仪式。不要为了显得工作复杂而创建空闲、重复或协调成本高于收益的子任务。

## 执行流程

### 1. 写文档前先调查

1. 阅读 `AGENTS.md`、`CONTRIBUTING.md` 和作用于当前目录的其他仓库指令。
2. 检查 Git 状态，保留用户已有修改。
3. 先确认项目类型、主要入口、依赖清单、启动命令、配置方式和部署方式，再按实际需要盘点 README、`docs/`、changelog、ADR、schema、路由、测试和运维脚本。
4. 阅读真正控制行为的代码和配置。把现有文字视为待验证的说法，不能直接当成事实。
5. 识别真实读者：首次参与的开发者、维护者、运维人员、非技术用户和自动化 agent。只为实际存在的读者建文档。

建立或重构体系时，阅读 [references/documentation-system.md](references/documentation-system.md)。

### 2. 诊断文档与协作缺口

重点检查：

- 没有明确的人类入口或快速开始。
- README、聊天记录、devlog 和 changelog 同时充当事实来源。
- 业务规则埋在组件、prompt 或注释中。
- 代码读取的环境变量没有进入示例和配置说明。
- 部署、备份和回滚说明与自动化脚本不一致。
- 数据字段缺少类型、归属或迁移说明。
- 命令已经过期、具有破坏性、依赖特定平台或从未验证。
- 文档围绕文件堆放，而不是围绕协作者需要回答的问题组织。
- 使用陌生术语，却没有解释它解决什么问题以及读者下一步要做什么。
- 默认新参与者已经理解 Git、branch、PR、部署和回滚。
- 为简单项目加入大量模板、审批、branch 层级或自动化，维护成本高于实际收益。
- 项目使用 coding agent，却没有告诉它先读什么、哪些文件不能随意修改、完成后怎样验证。

用清楚的中文解释重要问题。首次出现陌生术语时，用一句话说明它是什么、为什么当前项目需要它。遇到破坏性或生产环境操作时，先纠正风险假设，与开发者确认目标、备份和恢复方式后再执行命令。

### 3. 设计最小可行体系

现有命名和约定合理时继续沿用。先满足人类入口、协作入口和变更日志这三个硬性最低要求；其他主题文档只有在仓库中确实存在需要长期归属的事实或流程时才新建。按下面三个层级逐步建设，不要一次性创建无用文档。

1. **基础层**：先让项目能够被理解、运行和追溯。必须包括 README 或等价入口、必要的协作入口、变更日志和文档导航；存在配置时再加入配置示例。
2. **项目层**：项目确实存在业务规则、数据、部署或用户操作时，再建立对应主题文档。
3. **进阶层**：出现多人协作、自动部署、并行任务或重复自动化需求后，再引入 PR、CI、worktree、ADR、Skill、Hook 等机制。

默认归属如下：

- `README.md`：项目定位、前置条件、快速开始、常用命令和文档导航。
- `AGENTS.md` 或 `CONTRIBUTING.md`：协作规则、仓库地图、阅读路径和文档同步约定。
- 架构文档：模块边界、状态与请求流程、存储选择和非显然约束。
- 业务规则：权限、不变量、计算、导出和业务流程行为。
- 数据模型：持久化实体、字段、关系、标识符和迁移。
- 配置文档：环境变量、默认值、生产必填项、密钥处理和外部服务。
- 运维文档：CI/CD、部署、备份、回滚、健康检查、日志和恢复边界。
- 用户指南：真实用户流程和权限，仅在非技术用户实际操作产品时创建。
- changelog：记录某次修改，不作为当前行为的唯一来源。
- ADR：记录需要长期保留取舍理由的重要架构决策。
- Git 协作说明：branch、worktree、PR、合并、发布和 hotfix 规则，可放在 `CONTRIBUTING.md`、`AGENTS.md` 或运维文档中。

不要创建空占位文档，也不要把同一条规则复制到多个文件；在其他位置链接到负责该事实的文档。

项目缺少 Git 协作规则、存在自动部署或需要并行任务时，阅读 [references/git-workflow.md](references/git-workflow.md)。根据团队规模和发布方式选择最小流程，不要默认套用传统 Git Flow。

项目由新手配合 coding agent 开发，用户询问上下文、项目规则、Skills、Plugins、权限、automation、Hooks、沙盒、thread、配置、团队实践或排障，或者用户不知道怎样安全地让 AI 修改项目时，阅读 [references/agent-collaboration.md](references/agent-collaboration.md)。先建立最简单的“先读、再改、验证、说明”习惯；只有重复需求已经出现时，才介绍进阶机制。

### 4. 面向缺乏协作经验的开发者实施

明确给出第一条操作路径：

1. 说明项目做什么。
2. 说明前置条件，不假设读者已经知道本地工具链。
3. 按正确顺序提供可直接执行的安装和启动命令。
4. 说明预期 URL、进程和成功信号。
5. 给出最短的成功路径，再提供按问题查阅的详细文档；不要要求新手一开始读完整套体系。
6. 把每类修改路由到修改前必须阅读、修改后必须同步的文档。
7. 区分开发默认值与生产要求。
8. 在命令前说明风险、备份方式和失败后的恢复方法。
9. 只有当前任务确实用到时，才解释 branch、PR、worktree、CI 等概念，并给出当前项目可执行的下一步。

创建或大幅改写文档时，参考 [references/templates.md](references/templates.md)。保留有价值的旧内容，把旧笔记迁移到长期文档；旧路径很可能仍被引用时，保留简短兼容指引。

不要在文档中写入凭证、token、private key、密码或生产密钥。只记录变量名以及申请、配置或轮换方法。

### 5. 同步维护

在同一个任务中执行对应更新：

- 架构或跨模块流程变化：更新架构文档。
- 业务行为、权限、校验或导出变化：更新业务规则，并同步用户可见说明。
- 字段、schema、集合或迁移变化：更新数据模型。
- 环境变量变化：同时更新环境变量示例和配置文档。
- CI/CD、部署、备份、回滚或监控变化：更新运维文档。
- 用户操作流程变化：更新用户指南。
- branch、PR、worktree、发布或保护规则变化：更新 Git 协作说明和相关自动化。
- 项目已经使用 coding agent 规则、Hook 或 automation 时：对应更新协作说明和实际配置；项目未使用时不要为了完整性强行创建。
- 每次实质写入都记录本次修改；同时按影响更新负责当前行为的长期文档。仓库没有 changelog 时，先建立最小 changelog，不得以“仓库尚无约定”为由跳过。

除非发布工具会自动更新，否则不要在文档中硬编码“当前版本”。

### 6. 验证事实与导航

存在 Python 时运行审计脚本：

```bash
python scripts/audit_docs.py <repository-root>
```

从本 Skill 目录定位脚本，不要假设脚本位于目标仓库。脚本检查本地 Markdown 链接，并比较常见环境变量读取点与仓库中的环境变量示例。

还要人工验证：

1. 按风险比例运行或检查文档中的安装、构建、测试、迁移和健康检查命令。
2. 比较 API 摘要与实际路由注册。
3. 比较配置文档与代码、工作流和环境变量示例。
4. 比较部署、备份和回滚说明与自动化脚本的准确步骤。
5. 从链接所在文件出发验证相对路径。
6. 项目已经采用 Git 协作流程时，比较说明与 branch protection、CI 和部署触发条件。
7. 存在 Git 时运行仓库格式检查和 `git diff --check`。
8. 对照本次 diff 检查 changelog 是否覆盖日期、问题、改动、验证和影响范围，并确认所有新增或修改的 Markdown 相对链接可解析。

只检查了仓库文件时，不要声称已经验证生产环境。明确说明证据边界。

### 7. 清楚交付

先说明形成了什么文档与协作体系，再总结：

- 入口和单一事实来源的归属。
- 修正的重要不一致。
- 新手接下来最应该做的一到三个具体动作。
- 项目已经采用 Git 或 coding agent 协作规则时，说明选择理由；未采用时不要制造必须立即配置的待办。
- 已完成的验证和没有运行的检查。
- 仍需开发者、运维人员或生产权限确认的事实。
- 本次变更日志的实际路径；如果本次是纯只读任务，明确说明没有产生需要记录的仓库改动。

不要用流程仪式压垮缺乏经验的开发者。只有项目确实需要时，才加入模板、ADR 系统、PR 清单、release branch 等机制。

## 资源

- 建立或重构文档体系时阅读 [references/documentation-system.md](references/documentation-system.md)。
- 创建或大幅改写文档时阅读 [references/templates.md](references/templates.md)。
- 设计 branch、worktree、PR 和发布流程时阅读 [references/git-workflow.md](references/git-workflow.md)。
- 帮助新手与 coding agent 协作，或项目已经出现相关问题时阅读 [references/agent-collaboration.md](references/agent-collaboration.md)。
- 运行 `scripts/audit_docs.py`，重复检查链接和环境变量文档覆盖情况。
